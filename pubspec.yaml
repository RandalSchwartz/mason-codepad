name: codepad_integration_tests
description: Integration test suite for codepad brick generator.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  mason: ">=0.1.0 <0.2.0"
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.8
  very_good_analysis: ^6.0.0
