name: Gemini Code Review

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    permissions:
      contents: read
      pull-requests: write
    uses: RandalSchwartz/.github/.github/workflows/gemini-code-review.yml@main
    secrets: inherit
